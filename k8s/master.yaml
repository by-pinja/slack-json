apiVersion: v1
kind: Namespace
metadata:
  name: slack-integration

---

apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  labels:
    app: slack-integration-master
    version: master
  name: slack-integration-master
  namespace: slack-integration
spec:
  replicas: 1
  selector:
    matchLabels:
      app: slack-integration-master
      version: master
  template:
    metadata:
      labels:
        app: slack-integration-master
        version: master
      name: slack-integration-master
    spec:
      containers:
      - image: eu.gcr.io/ptcs-docker-registry/slack-integration:latest-master
        imagePullPolicy: Always
        name: slack-integration-master

---

apiVersion: v1
kind: Service
metadata:
  labels:
    app: slack-integration-master
    version: master
  name: slack-integration-master
  namespace: slack-integration
spec:
  type: ClusterIP
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: slack-integration-master
    version: master

---

apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: slack-integration-master-ingress
  namespace: slack-integration
  annotations:
    kubernetes.io/ingress.class: traefik
  labels:
    traefik-type: public
spec:
  rules:
    - host: slack-integration-master.protacon.cloud
      http:
        paths:
          - backend:
              serviceName: slack-integration-master
              servicePort: 80
  tls:
    - hosts:
      - slack-integration-master.protacon.cloud